Drop photos in this folder (jpg, jpeg, png, webp). The build resizes them and adds them to the Portfolio page automatically.

The file name becomes the caption: `matterhorn-at-sunrise.jpg` shows as "Matterhorn at sunrise".
Newest photos show first (by the date in the photo's EXIF data, or the date it was added to the site).
